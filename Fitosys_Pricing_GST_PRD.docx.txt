FITOSYSPricing Architecture & GST ComplianceProduct Requirements DocumentSTARTERâ‚¹999BASICâ‚¹1,499PROâ‚¹2,999STUDIOâ‚¹5,999Version 1.0  |  March 2026  |  Internal Use Only1. Strategic ContextMarket research identifies 300Kâ€“400K micro-coaches in India currently using spreadsheets and WhatsApp with no SaaS option under $15/month. The existing Fitosys Basic tier at â‚¹1,499/month sits above this gap. A client-count based pricing model closes this gap, captures the micro-coach entry market, and creates a natural land-and-expand motion as coaches grow their client base.Core insight from market research: The $5â€“15/month micro-coach segment represents 300Kâ€“400K coaches.No SaaS vendor currently offers true entry-tier automation at this price point.Capturing 10% of this segment = 30,000 coaches = â‚¹2.99 Cr MRR at Starter pricing alone.Land and expand: a coach who starts at Starter with 8 clients and grows to 18 clientsupgrades naturally to Basic without a sales conversation.1.1 Why Client Count, Not Feature CountFeature-gated pricing forces coaches to evaluate abstract capabilities they have not yet needed. Client-count pricing maps directly to a number every coach already tracks â€” how many clients they have. The upgrade moment is self-evident: when a coach hits the limit, they are by definition earning more revenue and can afford the next tier. No sales required.1.2 Why GST Compliance in Phase 1GST invoice generation is not a feature â€” it is a trust signal. An Indian coach paying â‚¹999/month to a SaaS platform will immediately ask for a GST invoice for their business accounting. The absence of this makes Fitosys feel informal and untrustworthy regardless of how well the product works. It is a 2-hour build. It should ship with the first paying coach, not after.Competitive advantage: WellnessZ and Picky Assist offer GST compliance. Global platforms (Trainerize,My PT Hub) do not. Building it in Phase 1 closes a trust gap that global competitors cannot close quickly.2. Pricing ArchitectureSTARTERâ‚¹999/monthUp to 10 clientsEntry point for micro-coaches. Land and expand.INCLUDEDâœ“WhatsApp check-ins â€” automated, every Sunday, in coach's nameâœ“Razorpay payment onboarding â€” single link, intake + paymentâœ“Renewal reminders â€” T-7 days, WhatsApp, automatedâœ“GST invoice generation â€” auto-generated on every paymentâœ“Coach dashboard â€” client list, enrollment status, payment historyâœ“Razorpay webhook integration â€” client auto-created on paymentâœ“WhatsApp webhook â€” client replies stored automaticallyLOCKED â€” UPGRADE TO UNLOCKðŸ”’  AI Monday summary (available on Basic and above)ðŸ”’  Custom check-in questions (available on Pro and above)ðŸ”’  Multi-location management (available on Studio)ðŸ”’  Priority support (available on Pro and above)BASICâ‚¹1,499/monthUp to 25 clientsGrowing coach. AI-powered insights every Monday.INCLUDEDâœ“Everything in Starterâœ“AI Monday summary â€” Gemini-powered, delivered via WhatsAppâœ“Risk scoring â€” clients flagged as At Risk, Engaged, or Strongâœ“Coach insight â€” one specific weekly priority surfaced by AIâœ“Extended client history â€” full check-in and payment timelineLOCKED â€” UPGRADE TO UNLOCKðŸ”’  Custom check-in questions (available on Pro and above)ðŸ”’  Multi-location management (available on Studio)ðŸ”’  Priority support (available on Pro and above)PROâ‚¹2,999/monthUp to 50 clientsEstablished coach. Full customisation and priority support.INCLUDEDâœ“Everything in Basicâœ“Custom check-in questions â€” up to 6 questions per programâœ“Program-specific check-in templates â€” different questions per program typeâœ“Priority WhatsApp support â€” response within 4 hoursâœ“Advanced renewal analytics â€” conversion rate, average renewal lagâœ“Churn reason logging and exportLOCKED â€” UPGRADE TO UNLOCKðŸ”’  Multi-location management (available on Studio)ðŸ”’  White-label onboarding page (available on Studio)ðŸ”’  API access (available on Studio)STUDIOâ‚¹5,999/monthUp to Unlimited clientsCertification bodies, aggregator partners, multi-location coaches.INCLUDEDâœ“Everything in Proâœ“Unlimited clients â€” no capâœ“Multi-location management â€” separate client lists per locationâœ“White-label onboarding page â€” custom domain, coach's own brandingâœ“API access â€” for certification body and aggregator integrationsâœ“Dedicated onboarding support â€” 1-hour setup call includedâœ“Custom WhatsApp template names â€” coach's own template brandingâœ“Priority support â€” response within 1 hourâœ“Quarterly business review â€” revenue and retention analysis2.1 Feature Comparison MatrixFeatureSTARTERBASICPROSTUDIOClient limit102550UnlimitedMonthly priceâ‚¹999â‚¹1,499â‚¹2,999â‚¹5,999WhatsApp check-insâœ“âœ“âœ“âœ“Razorpay onboardingâœ“âœ“âœ“âœ“Renewal remindersâœ“âœ“âœ“âœ“GST invoice generationâœ“âœ“âœ“âœ“Coach dashboardâœ“âœ“âœ“âœ“AI Monday summaryâœ—âœ“âœ“âœ“AI risk scoringâœ—âœ“âœ“âœ“Custom check-in questionsâœ—âœ—âœ“âœ“Program-specific templatesâœ—âœ—âœ“âœ“Advanced renewal analyticsâœ—âœ—âœ“âœ“Multi-location managementâœ—âœ—âœ—âœ“White-label onboarding pageâœ—âœ—âœ—âœ“API accessâœ—âœ—âœ—âœ“SupportEmail 48hEmail 24hWhatsApp 4hWhatsApp 1h2.2 Annual Pricing (2 months free)TierMonthlyAnnualSavingStarterâ‚¹999/monthâ‚¹9,990/yearâ‚¹1,998Basicâ‚¹1,499/monthâ‚¹14,990/yearâ‚¹2,998Proâ‚¹2,999/monthâ‚¹29,990/yearâ‚¹5,998Studioâ‚¹5,999/monthâ‚¹59,990/yearâ‚¹11,998Annual pricing strategy: 2 months free = 16.6% discount.Prioritise monthly at launch â€” reduces commitment friction for first 50 beta coaches.Introduce annual at Month 3 once retention data confirms coach satisfaction.2.3 Land and Expand ModelThe upgrade journey is mechanical, not sales-driven. When a coach hits their client limit, the dashboard shows a clear upgrade prompt. No email sequences. No sales calls. The product enforces the upgrade moment.TriggerFrom â†’ ToWhat happensCoach reaches 10 clientsStarter â†’ BasicDashboard shows upgrade prompt. New client onboarding blocked until upgrade. 7-day grace period.Coach reaches 25 clientsBasic â†’ ProSame prompt. Coaching continues for existing clients. New onboarding blocked.Coach reaches 50 clientsPro â†’ StudioSame prompt. Studio pitch includes dedicated onboarding call.Certification body enquiryAny â†’ StudioDirect Studio pitch. White-label page + API access are the selling points.Grace period: 7 days after limit hit before new onboarding is blocked.Existing clients are never disrupted â€” only new onboarding is gated.Downgrade: allowed at end of billing cycle. Client records retained for 90 days.3. Database Schema ChangesThese changes are additive to the existing schema defined in PRD v2. No existing tables are modified. Three new tables and two new columns.3.1 Modify coaches table-- Add to existing coaches tableALTER TABLE coaches ADD COLUMN plan TEXT DEFAULT 'trial'  CHECK (plan IN ('trial', 'starter', 'basic', 'pro', 'studio'));ALTER TABLE coaches ADD COLUMN plan_client_limit INTEGER DEFAULT 10;-- Set automatically by application on plan change-- starter=10, basic=25, pro=50, studio=NULL (unlimited)ALTER TABLE coaches ADD COLUMN plan_started_at TIMESTAMPTZ;ALTER TABLE coaches ADD COLUMN plan_billing_cycle TEXT DEFAULT 'monthly'  CHECK (plan_billing_cycle IN ('monthly', 'annual'));ALTER TABLE coaches ADD COLUMN next_billing_date DATE;ALTER TABLE coaches ADD COLUMN gst_numberTEXT;        -- coach's own GST numberALTER TABLE coaches ADD COLUMN business_nameTEXT;    -- for GST invoicesALTER TABLE coaches ADD COLUMN billing_addressTEXT;  -- for GST invoices3.2 New table: subscriptionsCREATE TABLE subscriptions (  id                    UUID PRIMARY KEY DEFAULT gen_random_uuid(),coach_id              UUID REFERENCES coaches(id) ON DELETE CASCADE NOT NULL,  plan                  TEXT NOT NULL CHECK (plan IN ('starter','basic','pro','studio')),billing_cycle         TEXT NOT NULL DEFAULT 'monthly',amount_inrDECIMAL(10,2) NOT NULL,gateway_payment_idTEXT,          -- Razorpay subscription IDgateway_order_id      TEXT,  status                TEXT DEFAULT 'active'                        CHECK (status IN ('active','cancelled','past_due','paused')),started_at            TIMESTAMPTZ DEFAULT NOW(),current_period_start  DATE NOT NULL,current_period_end    DATE NOT NULL,cancelled_at          TIMESTAMPTZ,cancel_reason         TEXT,created_at            TIMESTAMPTZ DEFAULT NOW());CREATE INDEX idx_subscriptions_coach_id ON subscriptions(coach_id);CREATE INDEX idx_subscriptions_status   ON subscriptions(status);3.3 New table: gst_invoicesCREATE TABLE gst_invoices (  id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),coach_id            UUID REFERENCES coaches(id) NOT NULL,client_id           UUID REFERENCES clients(id) NOT NULL,enrollment_id       UUID REFERENCES enrollments(id),payment_id          UUID REFERENCES payments(id) NOT NULL,invoice_number      TEXT UNIQUE NOT NULL,   -- format: FIT-YYYY-NNNNNNinvoice_date        DATE NOT NULL,  -- Coach (seller) details â€” from coaches tableseller_name         TEXT NOT NULL,seller_gstinTEXT,           -- optional: coach may not be GST registeredseller_address      TEXT NOT NULL,  -- Client (buyer) detailsbuyer_name          TEXT NOT NULL,buyer_email         TEXT NOT NULL,buyer_gstinTEXT,           -- optional  -- Line itemitem_description    TEXT NOT NULL,  -- e.g. '12-Week Nutrition Program'amount_before_taxDECIMAL(10,2) NOT NULL,gst_rateDECIMAL(5,2) DEFAULT 18.00,   -- 18% GST on SaaS servicescgst_amountDECIMAL(10,2),  -- Central GST (9% if intrastate)sgst_amountDECIMAL(10,2),  -- State GST (9% if intrastate)igst_amountDECIMAL(10,2),  -- Integrated GST (18% if interstate)total_amountDECIMAL(10,2) NOT NULL,  currency            TEXT DEFAULT 'INR',  -- Deliverypdf_urlTEXT,           -- Supabase Storage URLsent_to_client      BOOLEAN DEFAULT FALSE,sent_to_coach       BOOLEAN DEFAULT FALSE,created_at          TIMESTAMPTZ DEFAULT NOW());CREATE INDEX idx_gst_invoices_coach_id ON gst_invoices(coach_id);CREATE INDEX idx_gst_invoices_client_id ON gst_invoices(client_id);3.4 New table: checkin_templatesRequired for Pro and Studio tiers â€” custom check-in questions per program.CREATE TABLE checkin_templates (  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),coach_id    UUID REFERENCES coaches(id) ON DELETE CASCADE NOT NULL,program_id  UUID REFERENCES programs(id) ON DELETE CASCADE,  name        TEXT NOT NULL,  questions   JSONB NOT NULL,  -- JSONB structure:  -- [  --   { "order": 1, "text": "Energy this week (1-10)?", "type": "scale" },  --   { "order": 2, "text": "Sessions completed?",        "type": "number" },  --   { "order": 3, "text": "One win this week?",          "type": "text" },  --   { "order": 4, "text": "Anything challenging you?",   "type": "text" }  -- ]  -- Maximum 6 questions per template (Pro/Studio only)is_default  BOOLEAN DEFAULT FALSE,created_at  TIMESTAMPTZ DEFAULT NOW());3.5 RLS Policies for new tablesALTER TABLE subscriptions    ENABLE ROW LEVEL SECURITY;ALTER TABLE gst_invoices     ENABLE ROW LEVEL SECURITY;ALTER TABLE checkin_templates ENABLE ROW LEVEL SECURITY;CREATE POLICY coaches_own_subscriptions ON subscriptions  FOR ALL USING (coach_id = auth.uid());CREATE POLICY coaches_own_gst_invoices ON gst_invoices  FOR ALL USING (coach_id = auth.uid());CREATE POLICY coaches_own_checkin_templates ON checkin_templates  FOR ALL USING (coach_id = auth.uid());4. GST Invoice Generation â€” Feature SpecGST invoice generation fires automatically on every successful Razorpay payment. No manual action required from coach or client. The invoice is generated as a PDF, stored in Supabase Storage, and delivered via email to both client and coach within 60 seconds of payment.GST rate on coaching services: 18% (Education/Fitness services â€” confirm with compliance consultant).CGST 9% + SGST 9% for intrastate transactions.IGST 18% for interstate transactions.Coach's own GST number is optional â€” many independent coaches are not GST registered under â‚¹20L threshold.Invoice is required regardless of whether coach has GST number. It is a payment receipt.4.1 Invoice Number Format// Format: FIT-{YEAR}-{6-digit sequential number}// Examples://   FIT-2026-000001   (first ever invoice)//   FIT-2026-000142   (142nd invoice in 2026)//   FIT-2027-000001   (resets at start of each year)// Generation function in lib/gst/invoice-number.tsasync function generateInvoiceNumber(): Promise<string> {const year = new Date().getFullYear();const{ count } = await supabase.from('gst_invoices').select('*', { count: 'exact', head: true }).gte('invoice_date', `${year}-01-01`);constseq = String((count || 0) + 1).padStart(6, '0');  return `FIT-${year}-${seq}`;}4.2 Invoice PDF ContentSectionContentHeaderFitosys logo + 'TAX INVOICE' label + Invoice number + Invoice dateSeller (Coach)Business name (or full name) + Address + GSTIN (if registered) + PAN (optional)Buyer (Client)Full name + Email + GSTIN (if provided â€” most clients will not have one)Line ItemProgram name + Duration + Amount before taxTax BreakdownCGST 9% + SGST 9% (intrastate) OR IGST 18% (interstate) + TotalPayment DetailsRazorpay payment ID + Payment method + Paid dateFooter'This is a computer-generated invoice and does not require a signature.' + Fitosys support email4.3 API Routes for GST// New routes requiredGET  /api/invoices                    -- List all GST invoices for coachGET  /api/invoices/:id                -- Single invoice with PDF URLGET  /api/invoices/:id/download       -- Redirect to Supabase Storage PDFPOST /api/invoices/generate/:paymentId -- Manual regeneration (admin use only)// Internal lib function â€” called by Razorpay webhook, not exposed as route// lib/gst/generate-invoice.tsasync function generateGSTInvoice(paymentId: string): Promise<string>// Returns: PDF URL in Supabase Storage4.4 PDF Generation StackUse the @react-pdf/renderer library to generate invoice PDFs server-side inside a Next.js API route. Store the output in Supabase Storage under /invoices/{coach_id}/{invoice_number}.pdf. Deliver via Resend to both client and coach.// Installnpm install @react-pdf/renderer// lib/gst/generate-invoice.ts â€” core flowimport { renderToBuffer } from '@react-pdf/renderer';import { InvoicePDF } from '@/components/pdf/InvoicePDF';export async function generateGSTInvoice(data: InvoiceData): Promise<string> {  // 1. Generate invoice numberconstinvoiceNumber = await generateInvoiceNumber();  // 2. Render PDF to bufferconstpdfBuffer = await renderToBuffer(<InvoicePDF data={data} />);  // 3. Upload to Supabase Storageconst path = `invoices/${data.coachId}/${invoiceNumber}.pdf`;const{ data: upload } = await supabase.storage.from('invoices').upload(path, pdfBuffer, { contentType: 'application/pdf' });  // 4. Get public URLconst{ data: urlData } = supabase.storage.from('invoices').getPublicUrl(path);  // 5. Create gst_invoices record  await supabase.from('gst_invoices').insert({ ...data, pdf_url: urlData.publicUrl });  // 6. Send via Resend  await sendInvoiceEmails(data, urlData.publicUrl);  return urlData.publicUrl;}4.5 Coach Settings for GSTAdd a GST Details section to /dashboard/settings. Fields are optional but prompted on first payment. If coach does not fill these in, invoice generates with their profile name and a placeholder address.FieldRequiredNotesBusiness nameOptionalDefaults to coach's full_name if blankGSTINOptional15-character format validated client-side: 22AAAAA0000A1Z5Billing addressOptionalUsed as seller address on invoiceState (for CGST/SGST vs IGST)OptionalDefaults to coach's registered statePAN numberOptionalShown on invoice if provided5. Plan Enforcement LogicEvery API route that creates a new client or enrollment must check the coach's active client count against their plan limit before proceeding. This is the core enforcement mechanism for the client-count pricing model.5.1 Plan Limit Check â€” lib/plans/check-limit.ts// lib/plans/check-limit.tsexport const PLAN_LIMITS = {  trial:   5,  starter: 10,  basic:   25,  pro:     50,  studio:  null,   // null = unlimited} as const;export async function checkClientLimit(coachId: string): Promise<{  allowed: boolean;currentCount: number;  limit: number | null;  plan: string;daysInGracePeriod: number | null;}> {  // 1. Get coach planconst{ data: coach } = await supabase.from('coaches').select('plan, plan_client_limit').eq('id', coachId).single();  // 2. Count active clientsconst{ count } = await supabase.from('clients').select('*', { count: 'exact', head: true }).eq('coach_id', coachId).eq('status', 'active');const limit = PLAN_LIMITS[coach.plan];  if (limit === null) return { allowed: true, currentCount: count, limit: null, plan: coach.plan, daysInGracePeriod: null };  // 3. Check grace period (7 days after limit hit)constoverLimit = count >= limit;  // Grace period logic: check when limit was first hit in subscriptions table  return {    allowed: !overLimit,   // blocked after grace periodcurrentCount: count,    limit,    plan: coach.plan,daysInGracePeriod: overLimit ?calculateGraceDaysRemaining(coachId) : null,  };}5.2 Enforcement PointsCheck limit at these exact points â€” nowhere else:POST /api/public/:slug/intake â€” before creating Razorpay order. Return 403 with upgrade message if over limit.POST /api/webhook/razorpay â€” before creating client record. If over limit and grace period expired: refund payment, send coach notification, do not create client.Dashboard â€” show client count vs limit on every page load. Red warning at 80% of limit. Upgrade prompt at 100%.Critical: never block an existing client's check-in, renewal, or AI summary due to plan limits.Limits only apply to NEW client onboarding.Existing clients are always served regardless of plan state.5.3 Upgrade Flow// When coach hits limit â€” dashboard shows upgrade CTA// Clicking upgrade triggers:POST /api/subscriptions/upgradeBody: { targetPlan: 'basic' | 'pro' | 'studio', billingCycle: 'monthly' | 'annual' }// Flow:// 1. Create Razorpay subscription for new plan amount// 2. On payment success: update coaches.plan + coaches.plan_client_limit// 3. Create subscriptions record// 4. Send WhatsApp notification to coach confirming upgrade// 5. New client onboarding unblocked immediately6. New API Routes6.1 Subscription RoutesGET  /api/subscriptions/current     -- Current plan, billing date, client count vs limitPOST /api/subscriptions/upgrade     -- Upgrade to higher planPOST /api/subscriptions/cancel      -- Cancel at end of billing periodGET  /api/subscriptions/history     -- All past subscription records6.2 Invoice RoutesGET  /api/invoices                  -- List all invoices (paginated)GET  /api/invoices/:id              -- Single invoice recordGET  /api/invoices/:id/download     -- Redirect to PDF in Supabase StoragePUT  /api/coaches/billing           -- Update GST number, business name, address6.3 Check-in Template Routes (Pro and Studio only)GET    /api/checkin-templates              -- List templates for coachPOST   /api/checkin-templates              -- Create new template (max 6 questions)PUT    /api/checkin-templates/:id          -- Update templateDELETE /api/checkin-templates/:id          -- Delete templatePOST   /api/checkin-templates/:id/assign   -- Assign template to program7. Dashboard Additions7.1 Plan Status WidgetAdd to /dashboard home page. Shows current plan, client count vs limit, days to next billing, and upgrade CTA when within 20% of limit.// GET /api/subscriptions/current response shape{  plan: 'starter',billing_cycle: 'monthly',amount_inr: 999,current_period_end: '2026-04-08',active_clients: 8,client_limit: 10,utilisation_pct: 80,       // 8/10 = 80%show_upgrade_prompt: true, // true when >= 80%days_remaining: 31,}7.2 Invoices PageAdd /dashboard/invoices as a new navigation item. Lists all GST invoices with download links. Filterable by month. Shows total revenue collected this month.RoutePage/dashboard/invoicesInvoice list â€” month filter, download links, total amount/dashboard/settings/billingGST details â€” business name, GSTIN, address, PAN8. Build Sequence â€” Additions to PRD v2These tasks extend the 8-task Antigravity sequence defined in PRD v2. Run them in order, after the core 8 tasks complete.TaskModuleAntigravity Prompt Summary9Plan enforcement middlewareCreate lib/plans/check-limit.ts with PLAN_LIMITS constants. Add limit check to POST /api/public/:slug/intake â€” return 403 with upgrade_required: true if over limit. Add to Razorpay webhook handler before client creation.10GST invoice generationInstall @react-pdf/renderer. Create lib/gst/generate-invoice.ts. Create components/pdf/InvoicePDF.tsx with all fields from Section 4.2. Create Supabase Storage bucket 'invoices'. Wire generateGSTInvoice() call into Razorpay webhook after payment record creation. Create GET /api/invoices route.11Subscription managementCreate subscriptions table. Create POST /api/subscriptions/upgrade route wired to Razorpay. Create GET /api/subscriptions/current. Add plan status widget to /dashboard home page with upgrade CTA.12Billing settings pageCreate /dashboard/settings/billing page. Form: business name, GSTIN, billing address, PAN. Validate GSTIN format client-side. Save to coaches table. Used by invoice generator.13Invoices dashboard pageCreate /dashboard/invoices page. Fetch from GET /api/invoices. Show invoice number, date, client name, amount, download link. Month filter. Total revenue summary at top.14Check-in templates (Pro/Studio)Create checkin_templates table. Create CRUD routes. Create /dashboard/settings/checkin-templates page. Gate behind plan check â€” return 403 for Starter and Basic. Wire template selection into weekly check-in cron.9. Revenue ProjectionsConservative projections based on 50 beta coaches in Month 1 growing to 200 coaches by Month 6. These are internal planning numbers â€” not for investor use without validation.MonthCoachesAvg ARPUMRRNotesMonth 115â‚¹1,200â‚¹18,000Beta launch. Mix of Starter and Basic.Month 235â‚¹1,300â‚¹45,500Word of mouth + certification body outreach.Month 365â‚¹1,400â‚¹91,000First coaches upgrading from Starter to Basic.Month 4100â‚¹1,500â‚¹1,50,000First partnership converts a batch of coaches.Month 5140â‚¹1,600â‚¹2,24,000Pro tier coaches growing. Annual plan introduced.Month 6200â‚¹1,700â‚¹3,40,000â‚¹34 Lakh ARR run rate. First Studio tier coaches.â‚¹3,40,000 MRR at Month 6 = â‚¹40.8 Lakh ARR.Break-even: approximately Month 3 at 65 coaches covering infrastructure costs.Infrastructure cost at 200 coaches: ~â‚¹8,000â€“12,000/month (Supabase, Vercel, Resend, Meta messaging).These projections assume no paid marketing â€” organic + partnership only.Fitosys Pricing & GST PRD v1.0  |Alchemetryx  |  March 2026  |  Confidential
