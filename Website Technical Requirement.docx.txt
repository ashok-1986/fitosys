Fitosys Website â€” Technical Requirements SpecificationDocument scope: Landing page / marketing website at fitosys.alchemetryx.com. Not the coach dashboard. Not the client intake form. This document governs only the public-facing marketing site.1. Layout & StructureTR-01 â€” Full-viewport sections Every section occupies exactly 100vh minimum height. No section breaks mid-viewport. On mobile, sections may extend beyond 100vh if content demands it â€” they must never compress below it. Use min-height: 100vh not height: 100vh to avoid overflow traps on mobile browsers with dynamic toolbars.TR-02 â€” Single-page architecture All sections live in one HTML document. Navigation anchors scroll to sections. No page reloads, no routing. This reduces bounce from slow connections â€” critical for Indian Tier 2 mobile users on 4G.TR-03 â€” Fixed navigation Nav stays fixed at top across all scroll positions. It becomes opaque with backdrop blur after 60px of scroll. Nav height is 64px on desktop, 56px on mobile. All section padding accounts for nav height so anchor links don't clip headings.TR-04 â€” Z-index architecture Defined stack from top to bottom: mobile menu overlay (999), nav bar (100), GSAP animated elements (10â€“50), section content (2), section backgrounds and decorative elements (0â€“1). No z-index values outside this system.2. Visual DesignTR-05 â€” Minimal design principle Each section contains one primary message, one supporting element, and one CTA. Nothing else. No secondary CTAs competing with the primary. No decorative elements that don't serve meaning. Every visual element must justify its presence.TR-06 â€” White/dark space usage Minimum section padding: clamp(80px, 10vw, 140px) top and bottom. Minimum horizontal gutter: clamp(24px, 5vw, 80px). Content columns never exceed 1280px wide. Body copy columns never exceed 560px wide â€” long lines destroy readability on dark backgrounds.TR-07 â€” Colour discipline Red (#F20000) appears on no more than 2â€“3 elements per section. It is reserved for CTAs, active states, and one key data point. It is never used as a background fill for large areas except the CTA banner section. Every other surface is #0A0A0A, #111111, or #141414.TR-08 â€” No images above the foldThe hero section uses no raster images. Only CSS, SVG, and HTML-rendered UI mockups. This eliminates the largest LCP bottleneck. Images are only used in testimonial avatars (SVG initials acceptable as replacement) and optional photography sections below the fold.TR-09 â€” One decorative element per section One allowed per section: a grid background, a large ghost text watermark, a gradient glow, or a texture overlay. Never two in the same section. Never animated and static decorative elements together.3. TypographyTR-10 â€” Font system Two fonts only. Barlow Condensed (display) and Urbanist (body). No system fallback fonts rendered in production. Both loaded via Google Fonts with display=swap to prevent FOIT.TR-11 â€” Barlow Condensed constraints Maximum weight: 500. Minimum size: 12px. Always uppercase. Letter-spacing minimum +0.04em. Line-height minimum 1.05 for multi-line display text. Never used for body copy, captions, or anything below 12px.TR-12 â€” Urbanist usage All body copy, labels, buttons, nav links, form inputs, tags, and UI micro-text use Urbanist. Weights in use: 400 (body), 500 (labels), 600 (subheadings), 700 (CTAs and strong emphasis only). Line-height minimum 1.65 for paragraph text, 1.5 for short labels.TR-13 â€” Type scale Defined scale only â€” no arbitrary font sizes. Scale: 11px (micro labels), 13px (small UI), 14px (captions), 16px (body), 18px (sub-body), clamp(38px, 5vw, 68px) (section titles), clamp(52px, 6vw, 96px) (hero title). Nothing outside this scale without a documented reason.TR-14 â€” Readability floor No body text below 14px rendered to users. No line-length above 70 characters for paragraph text. All paragraph text colour minimum #A0A0A0 on dark backgrounds â€” never below this contrast level.4. Animation & InteractionTR-15 â€” GSAP as the sole animation library All scroll-triggered animations, entrance effects, and micro-interactions use GSAP + ScrollTrigger. No CSS @keyframes for scroll-triggered content (only for looping ambient animations like the ticker or bar pulse). No mixing of animation libraries.TR-16 â€” Animation budget per section Maximum 3 GSAP animations triggered per section on scroll entry. Stagger delays between sibling elements: 0.08sâ€“0.15s. No animation duration above 1.2s. Ease functions: power3.out for entrances, power2.inOut for transitions, sine.inOut for loops.TR-17 â€” Reduced motion respect All GSAP scroll animations must check prefers-reduced-motion. If set, skip animation and show elements at final state immediately. Implementation: wrap all ScrollTrigger callbacks in a matchMedia check.const mm = gsap.matchMedia();mm.add("(prefers-reduced-motion: no-preference)", () => {  // all scroll animations here});TR-18 â€” Ticker animationThe pain-points ticker uses pure CSS animation only (translateX keyframe). Never GSAP â€” it runs continuously and GSAP overhead on a looping element causes frame drops on low-end Android devices.TR-19 â€” No animation on first paint Hero text entrance begins at 200ms delay minimum after page load. No animation fires before fonts are confirmed loaded (document.fonts.ready). This prevents FOUT-triggered layout shifts during animation.TR-20 â€” Phone mockup tab switching Tab switches use CSS opacity + translateX transition (0.4s ease). GSAP is only used for the subtle pulse on float cards when tabs change. Auto-cycle interval is 5000ms. Interval is cleared immediately on any user tab interaction and never reinstated.5. PerformanceTR-21 â€” Core Web Vitals targets LCP (Largest Contentful Paint): under 2.5s on a simulated 4G connection (25 Mbps). FID / INP: under 100ms. CLS (Cumulative Layout Shift): under 0.1. These are minimum pass thresholds â€” target green on PageSpeed Insights for both mobile and desktop.TR-22 â€” No render-blocking resources Google Fonts loaded with rel="preconnect" and display=swap. GSAP loaded from CDN with defer. No synchronous scripts in <head>. CSS is inline in <style> block (single-file implementation) â€” no external stylesheet requests.TR-23 â€” Image optimisation (when used) All photography below the fold uses loading="lazy". Maximum image width served: 1200px. Format: WebP with JPEG fallback. No image file above 200KB uncompressed. Testimonial avatars use CSS-rendered initials â€” zero image requests.TR-24 â€” No third-party scripts No analytics, chat widgets, or marketing pixels on the initial load. If analytics are added later (PostHog or Plausible recommended â€” both privacy-first, lightweight), they load after window.onload with a 3-second delay. No Google Tag Manager, no Meta Pixel, no Hotjar on this page.TR-25 â€” Single HTTP request for CSS + JS The marketing site is a single .html file. All CSS is inline. GSAP loads from a CDN. Target: total page weight under 150KB excluding fonts and GSAP library.6. ResponsivenessTR-26 â€” Breakpoint system Three breakpoints only. Desktop: 1025px and above. Tablet: 769pxâ€“1024px. Mobile: 768px and below. No intermediate breakpoints. Grid layouts collapse from multi-column to single-column at the tablet breakpoint.TR-27 â€” Mobile-first font sizing All font-size values use clamp() for fluid scaling between mobile and desktop. No fixed pixel sizes for display text. Body text is fixed at 16px on mobile (minimum readable on small screens), 17px on desktop.TR-28 â€” Touch targets All interactive elements (buttons, nav links, tab switchers, form inputs) have a minimum touch target of 44Ã—44px. This is Apple HIG standard and prevents mis-taps on mobile â€” relevant since Indian coaches are predominantly mobile users.TR-29 â€” Hero visual on mobileThe phone mockup and float cards are hidden on screens below 1024px (display: none). The hero left column takes full width. This is intentional â€” a complex animated visual on a small screen creates cognitive overload and slows first paint.TR-30 â€” Horizontal scroll prevention overflow-x: hidden on both html and body. All absolutely positioned elements (float cards, decorative watermarks) are confirmed to not create horizontal overflow at any breakpoint. Test specifically on 375px (iPhone SE) and 390px (iPhone 14).7. SEO & MetadataTR-31 â€” Semantic HTML structure One <h1> per page (hero title). Section headings use <h2>. Supporting headings use <h3>. No heading hierarchy skipped. Navigation in <nav>. Footer in <footer>. Main content in <main>. This structure is required for crawler legibility and accessibility.TR-32 â€” Meta tags Required tags: <title>, <meta name="description">, <meta name="keywords"> (secondary), <meta property="og:title">, <meta property="og:description">, <meta property="og:image"> (1200Ã—630px), <meta name="twitter:card">. All populated before launch.TR-33 â€” Canonical URL <link rel="canonical" href="https://fitosys.alchemetryx.com"> in <head>. Prevents duplicate content indexing if the site is accessible at multiple URLs (www vs non-www).TR-34 â€” India-specific keyword targeting Title tag and meta description must include at least two of: "India", "Indian coaches", "WhatsApp", "coaching software India", "yoga coach software". These are the search terms your primary persona (Priya, Mumbai-based) actually types.TR-35 â€” Structured data Add JSON-LD SoftwareApplication schema to <head>. Minimum fields: name, applicationCategory, operatingSystem, offers (price in INR), description. This enables rich results in Google Search for software product pages.8. AccessibilityTR-36 â€” Colour contrast All text on dark backgrounds meets WCAG AA minimum: 4.5:1 for body text, 3:1 for large text (above 18px or 14px bold). Steel grey (#A0A0A0) on black (#0A0A0A) passes at 5.74:1. Red (#F20000) on black passes at 3.68:1 for display text only â€” never use red for small body copy.TR-37 â€” Keyboard navigation All interactive elements are focusable via Tab key. Focus states are visible â€” styled with a 2px red outline offset by 3px (not the browser default). Mobile menu is operable without a mouse. Form fields have correct tab order.TR-38 â€” Alt text Every <img> tag has a non-empty alt attribute. Decorative SVGs use aria-hidden="true". The phone mockup and dashboard UI are marked with role="img" and aria-label="Fitosys dashboard preview".TR-39 â€” Form accessibility All form inputs are associated with <label> elements via for/id pairing. Required fields marked with aria-required="true". Error states use aria-describedby to link input to error message. The demo form is fully operable with screen readers.9. Hosting & DeploymentTR-40 â€” Deployment target fitosys.alchemetryx.com via Hostinger DNS â†’ Vercel. A record points to Vercel's IP. SSL certificate auto-provisioned by Vercel. HTTP â†’ HTTPS redirect enforced at the CDN level.TR-41 â€” Cache headers HTML: Cache-Control: no-cache (always fresh). CSS/JS assets: Cache-Control: public, max-age=31536000, immutable (content-hashed filenames). Fonts served by Google CDN â€” no control needed.TR-42 â€” Uptime target 99.9% monthly uptime. Vercel's global CDN satisfies this by default. No additional uptime monitoring required for the marketing site at MVP stage.10. Testing RequirementsTR-43 â€” Browser support Must render correctly on: Chrome 110+, Safari 16+, Firefox 115+, Samsung Internet 20+. Samsung Internet is the default browser on most Indian Android devices â€” it is not optional to test.TR-44 â€” Device testing minimum Physical device tests required before launch: iPhone 14 (390px), Samsung Galaxy A54 (412px â€” most common Indian mid-range), iPad Air (820px), MacBook 13". Simulator testing is not a substitute for physical device testing on font rendering and touch response.TR-45 â€” Performance audit Run Lighthouse audit in Chrome DevTools before launch. Pass criteria: Performance 85+, Accessibility 90+, Best Practices 90+, SEO 95+. Run on both mobile and desktop simulation. Document the score and any items below threshold with a resolution plan.TR-46 â€” Animation performance All GSAP animations must maintain 60fps on a mid-range Android device (Snapdragon 680 class). Test using Chrome DevTools Performance tab. Frame drops below 50fps during scroll animations are a blocking issue.Quick Reference SummaryIDCategoryRequirementTR-01Layout100vh min per sectionTR-05DesignOne message per sectionTR-06Design80â€“140px section paddingTR-08DesignNo raster images above foldTR-11TypographyBarlow max weight 500TR-12TypographyUrbanist for all body textTR-15AnimationGSAP only, no mixed librariesTR-17AnimationRespect prefers-reduced-motionTR-21PerformanceLCP under 2.5s on 4GTR-24PerformanceNo third-party scripts on loadTR-26ResponsiveThree breakpoints onlyTR-29ResponsiveHide phone mockup below 1024pxTR-36AccessibilityWCAG AA contrast on all textTR-40HostingVercel + Hostinger DNSTR-43TestingInclude Samsung InternetTR-45TestingLighthouse 85+ across categories46 requirements across 10 categories. Every one is specific, testable, and tied to either the brand, the audience, or the stack. Nothing is vague.Two I'd flag as the most commonly missed in practice: TR-17 (reduced motion) gets skipped on almost every GSAP-heavy site and becomes an accessibility liability, and TR-43 (Samsung Internet) â€” most Indian devs test on Chrome and miss Samsung-specific rendering differences entirely.
