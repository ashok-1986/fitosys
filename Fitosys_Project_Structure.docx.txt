FITOSYSModular Project StructureNext.js 14 App Router  |TypeScript  |Supabase  |Razorpay  |VercelVersion 1.0  |  March 2026  |Confidential  |  Alchemetryx1. Root Directory StructureThe project lives at D:\Projects\Personal_Projects\Websites\fitosys. Every file and folder below is relative to this root.fitosys/â”œâ”€â”€ app/                        # Next.js App Router â€” all pages and API routesâ”œâ”€â”€ components/                 # Shared UI componentsâ”œâ”€â”€ lib/                        # Business logic, API clients, utilitiesâ”œâ”€â”€ types/                      # TypeScript interfaces and type definitionsâ”œâ”€â”€ hooks/                      # Custom React hooksâ”œâ”€â”€ styles/                     # Global CSS (minimal â€” Tailwind handles most)â”œâ”€â”€ public/                     # Static assets (logo, favicon)â”œâ”€â”€supabase/                   # DB migrations and Supabase configâ”œâ”€â”€ .env.local                  # Local env vars â€” never commitâ”œâ”€â”€ .env.example                # Template for env vars â€” commit thisâ”œâ”€â”€ next.config.jsâ”œâ”€â”€ tailwind.config.jsâ”œâ”€â”€tsconfig.jsonâ”œâ”€â”€vercel.json                 # Cron job schedule configâ””â”€â”€package.json2. app/ Directory â€” Pages and API RoutesAll routes are collocated under app/. Pages use page.tsx. API routes use route.ts. Layouts use layout.tsx.app/â”‚â”œâ”€â”€layout.tsx                           # Root layout â€” fonts, metadata, providersâ”œâ”€â”€page.tsx                             # Landing page (public)â”œâ”€â”€ globals.css                          # Tailwind base imports onlyâ”‚â”œâ”€â”€ (auth)/                              # Route group â€” auth pages (no dashboard layout)â”‚â”œâ”€â”€ login/â”‚â”‚â””â”€â”€page.tsxâ”‚â””â”€â”€ signup/â”‚â””â”€â”€page.tsxâ”‚â”œâ”€â”€ (dashboard)/                         # Route group â€” protected coach dashboardâ”‚â”œâ”€â”€layout.tsx                       # Dashboard shell: sidebar + topbarâ”‚â”œâ”€â”€ dashboard/â”‚â”‚â””â”€â”€page.tsx                     # Home: stat cards, attention list, renewalsâ”‚â”œâ”€â”€ dashboard/clients/â”‚â”‚â”œâ”€â”€page.tsx                     # Client list tableâ”‚â”‚â””â”€â”€ [id]/â”‚â”‚â””â”€â”€page.tsx                 # Client profile: overview, check-ins, paymentsâ”‚â”œâ”€â”€ dashboard/programs/â”‚â”‚â””â”€â”€page.tsx                     # Programs list + add newâ”‚â”œâ”€â”€ dashboard/pulse/â”‚â”‚â””â”€â”€page.tsx                     # Weekly Pulse: AI summary + chartsâ”‚â”œâ”€â”€ dashboard/payments/â”‚â”‚â””â”€â”€page.tsx                     # Revenue chart + transaction listâ”‚â””â”€â”€ dashboard/settings/â”‚â””â”€â”€page.tsx                     # Profile, check-in config, billingâ”‚â”œâ”€â”€ join/â”‚â””â”€â”€ [slug]/â”‚â”œâ”€â”€page.tsx                     # Public intake form + Razorpay checkoutâ”‚â””â”€â”€ success/â”‚â””â”€â”€page.tsx                 # Post-payment confirmation (terminal page)â”‚â””â”€â”€api/                                 # All API routesâ”œâ”€â”€ auth/â”‚â”œâ”€â”€ signup/route.tsâ”‚â”œâ”€â”€ login/route.tsâ”‚â”œâ”€â”€ logout/route.tsâ”‚â””â”€â”€ me/route.tsâ”œâ”€â”€ coaches/â”‚â”œâ”€â”€ profile/route.tsâ”‚â”œâ”€â”€ settings/route.tsâ”‚â””â”€â”€ dashboard/route.tsâ”œâ”€â”€ programs/â”‚â”œâ”€â”€route.ts                     # GET list / POST createâ”‚â”œâ”€â”€ [id]/route.ts                # PUT edit / DELETE deactivateâ”‚â””â”€â”€ public/[slug]/route.ts       # Public: no auth requiredâ”œâ”€â”€ clients/â”‚â”œâ”€â”€route.ts                     # GET listâ”‚â””â”€â”€ [id]/â”‚â”œâ”€â”€route.ts                 # GET single / PUT updateâ”‚â”œâ”€â”€checkins/route.tsâ”‚â””â”€â”€ payments/route.tsâ”œâ”€â”€enrollments/â”‚â”œâ”€â”€route.tsâ”‚â”œâ”€â”€ expiring/route.tsâ”‚â””â”€â”€ [id]/churn/route.tsâ”œâ”€â”€checkins/â”‚â”œâ”€â”€ weekly/route.tsâ”‚â”œâ”€â”€ summary/[week]/route.tsâ”‚â””â”€â”€ trigger/route.ts             # Manual trigger for testingâ”œâ”€â”€ payments/â”‚â”œâ”€â”€route.tsâ”‚â”œâ”€â”€ create-order/route.tsâ”‚â””â”€â”€ create-renewal/route.tsâ”œâ”€â”€ public/â”‚â””â”€â”€ [slug]/â”‚â”œâ”€â”€route.ts                 # GET coach + programsâ”‚â””â”€â”€ intake/route.ts          # POST intake + create Razorpay orderâ”œâ”€â”€ webhook/â”‚â”œâ”€â”€razorpay/route.ts            # payment.captured handlerâ”‚â””â”€â”€whatsapp/route.ts            # GET verify + POST incoming messagesâ””â”€â”€cron/â”œâ”€â”€ weekly-checkin/route.tsâ”œâ”€â”€ renewal-check/route.tsâ”œâ”€â”€ renewal-followup/route.tsâ”œâ”€â”€monday-summary/route.tsâ””â”€â”€ expiry-check/route.ts3. lib/ Directory â€” Business Logic and API Clientslib/ contains all non-UI logic. Nothing in lib/ should import from app/ or components/. Every external service has its own subfolder.lib/â”‚â”œâ”€â”€supabase/â”‚â”œâ”€â”€client.ts                # Browser-safe Supabase client (uses anon key)â”‚â”œâ”€â”€server.ts                # Server-side Supabase client (uses service_role key)â”‚â””â”€â”€middleware.ts            # Session refresh for protected routesâ”‚â”œâ”€â”€razorpay/â”‚â”œâ”€â”€client.ts                # Razorpay SDK instanceâ”‚â”œâ”€â”€ create-order.ts          # Creates Razorpay order for intake flowâ”‚â”œâ”€â”€ create-renewal.ts        # Creates renewal payment linkâ”‚â””â”€â”€ verify-webhook.ts        # Signature verification for webhookâ”‚â”œâ”€â”€whatsapp/â”‚â”œâ”€â”€send.ts                  # Raw Meta Graph API call â€” never called directly by cronsâ”‚â””â”€â”€templates.ts             # Named builder functions â€” one per templateâ”‚                                #   sendCheckin(clientPhone, clientName, coachName)â”‚                                #   sendWelcome(clientPhone, coachName, clientName, program, startDate)â”‚                                #   sendRenewalInfo(clientPhone, ...)â”‚                                #   sendRenewalCTA(clientPhone, ...)â”‚                                #   sendCoachNotification(coachPhone, clientName, ...)â”‚                                #   sendMondaySummary(coachPhone, summaryText)â”‚â”œâ”€â”€gemini/â”‚â”œâ”€â”€client.ts                # Gemini API client â€” model config, retry logicâ”‚â”œâ”€â”€ weekly-summary.ts        # Generates Monday coach summaryâ”‚â”œâ”€â”€ risk-score.ts            # Scores client churn risk from check-in dataâ”‚â”œâ”€â”€ renewal-message.ts       # Generates personalised renewal message copyâ”‚â””â”€â”€ coach-insight.ts         # One-line coaching priority for the weekâ”‚â”œâ”€â”€ auth/â”‚â”œâ”€â”€ get-session.ts           # Server-side session helperâ”‚â””â”€â”€ require-auth.ts          # Throws if no session â€” use in API routesâ”‚â”œâ”€â”€cron/â”‚â””â”€â”€ verify-secret.ts         # Validates x-cron-secret headerâ”‚â””â”€â”€ utils/â”œâ”€â”€dates.ts                 # Date helpers: add weeks, format IST, get week numberâ”œâ”€â”€phone.ts                 # Phone number normalisation (remove +, add 91, etc.)â”œâ”€â”€slugify.ts               # Generates coach slug from nameâ””â”€â”€currency.ts             # Format â‚¹ amounts consistently4. components/ Directory â€” UI ComponentsAll components are in TypeScript. No default exports except for page.tsx files. Every component has a single responsibility.components/â”‚â”œâ”€â”€ui/                          # shadcn/ui base components (auto-generated)â”‚â”œâ”€â”€button.tsxâ”‚â”œâ”€â”€card.tsxâ”‚â”œâ”€â”€input.tsxâ”‚â”œâ”€â”€select.tsxâ”‚â”œâ”€â”€table.tsxâ”‚â”œâ”€â”€badge.tsxâ”‚â”œâ”€â”€dialog.tsxâ”‚â””â”€â”€ ...                      # Other shadcn components as neededâ”‚â”œâ”€â”€ dashboard/â”‚â”œâ”€â”€StatCard.tsx             # Reusable stat card: label + value + optional trendâ”‚â”œâ”€â”€AttentionList.tsx        # Clients needing attention â€” name, reason, actionsâ”‚â”œâ”€â”€RenewalList.tsx          # Upcoming renewals â€” client, program, days remainingâ”‚â”œâ”€â”€SummaryPreview.tsx       # Last week AI summary preview cardâ”‚â””â”€â”€Sidebar.tsx              # Dashboard navigation sidebarâ”‚â”œâ”€â”€ clients/â”‚â”œâ”€â”€ClientTable.tsx          # Sortable, filterable client listâ”‚â”œâ”€â”€ClientProfile.tsx        # Profile header with key statsâ”‚â”œâ”€â”€CheckinHistory.tsx       # Week-by-week check-in tableâ”‚â””â”€â”€PaymentHistory.tsx       # Transaction list for single clientâ”‚â”œâ”€â”€ programs/â”‚â”œâ”€â”€ProgramCard.tsx          # Program display: name, price, duration, toggleâ”‚â””â”€â”€ProgramForm.tsx          # Create/edit program formâ”‚â”œâ”€â”€ pulse/â”‚â”œâ”€â”€SummaryCard.tsx          # Full AI summary display with sectionsâ”‚â”œâ”€â”€ResponseRateChart.tsx    # 8-week response rate bar chart (recharts)â”‚â””â”€â”€WeekSelector.tsx         # Dropdown to view historical summariesâ”‚â”œâ”€â”€ intake/â”‚â”œâ”€â”€IntakeForm.tsx           # Client onboarding formâ”‚â”œâ”€â”€ProgramSelector.tsx      # Radio group: coach's active programs + pricesâ”‚â””â”€â”€RazorpayCheckout.tsx     # Loads checkout.js, opens modal, handles success/failâ”‚â”œâ”€â”€ shared/â”‚â”œâ”€â”€Logo.tsx                 # Fitosys logo componentâ”‚â”œâ”€â”€LoadingSpinner.tsxâ”‚â”œâ”€â”€EmptyState.tsx           # Empty state with CTAâ”‚â”œâ”€â”€ErrorBoundary.tsxâ”‚â””â”€â”€PageHeader.tsx           # Consistent page title + subtitle + optional actionâ”‚â””â”€â”€ forms/â”œâ”€â”€FormField.tsx            # Label + input + error message wrapperâ””â”€â”€SubmitButton.tsx         # Button with loading state5. types/ Directory â€” TypeScript InterfacesAll DB table interfaces live in types/database.ts. All API request/response shapes live in types/api.ts. Never use 'any'.types/â”œâ”€â”€database.ts                  # One interface per DB tableâ”‚                                #   Coach, Program, Client, Enrollmentâ”‚                                #   Checkin, AiSummary, Payment, WhatsappLogâ”œâ”€â”€api.ts                       # Request/response shapes for all API routesâ”œâ”€â”€whatsapp.ts                  # Meta webhook payload typesâ”œâ”€â”€razorpay.ts                  # Razorpay webhook payload typesâ””â”€â”€index.ts                     # Re-exports all types5.1 Key Type Definitions// types/database.tsexport interface Coach {  id: string  email: stringfull_name: stringwhatsapp_number: stringcountry_code: stringtimezone: stringcoaching_type: string[]  plan: 'trial' | 'basic' | 'pro'checkin_day: number          // 0=Sun, 1=Mon ... 6=Satcheckin_time: string         // '19:00'  slug: string  status: 'active' | 'inactive' | 'suspended'created_at: string}export interface Enrollment {  id: stringcoach_id: stringclient_id: string | null      // nullable until webhook firesprogram_id: stringstart_date: stringend_date: stringamount_paid: number  currency: stringgateway_payment_id: string | nullgateway_order_id: string | null  status: 'active' | 'expired' | 'renewed' | 'cancelled'  renewal_reminder_1_sent: boolean  renewal_reminder_2_sent: booleanchurn_reason: string | null}export interface Checkin {  id: stringcoach_id: stringclient_id: stringenrollment_id: string | nullweek_number: numbercheck_date: stringraw_reply: string | nullsessions_completed: number | nullenergy_score: number | null    // 1â€“10  notes: string | null  processed: booleanresponded_at: string | nullmessage_sent_at: string | null}6. supabase/ Directory â€” Database Migrationssupabase/â”œâ”€â”€ migrations/â”‚â”œâ”€â”€ 20260301_001_initial_schema.sql     # All CREATE TABLE statementsâ”‚â”œâ”€â”€ 20260301_002_rls_policies.sql        # All RLS policiesâ”‚â””â”€â”€ 20260301_003_indexes.sql             # Performance indexesâ””â”€â”€seed.sql                                 # Dev seed data (2 coaches, 5 clients each)6.1 Recommended Indexes-- Performance indexesCREATE INDEX idx_clients_coach_id         ON clients(coach_id);CREATE INDEX idx_clients_whatsapp         ON clients(whatsapp_number);CREATE INDEX idx_enrollments_coach_id     ON enrollments(coach_id);CREATE INDEX idx_enrollments_end_date     ON enrollments(end_date);CREATE INDEX idx_enrollments_status       ON enrollments(status);CREATE INDEX idx_checkins_coach_week      ON checkins(coach_id, week_number);CREATE INDEX idx_checkins_client_id       ON checkins(client_id);CREATE INDEX idx_payments_gateway_id      ON payments(gateway_payment_id);CREATE INDEX idx_whatsapp_log_client_id   ON whatsapp_log(client_id);7. Configuration Files7.1 vercel.json{  "crons": [{ "path": "/api/cron/weekly-checkin",   "schedule": "30 13 * * 0" },{ "path": "/api/cron/renewal-check",    "schedule": "0 10 * * *" },{ "path": "/api/cron/renewal-followup", "schedule": "0 11 * * *" },{ "path": "/api/cron/monday-summary",   "schedule": "30 1 * * 1"  },{ "path": "/api/cron/expiry-check",     "schedule": "0 0 * * *"   }  ]}7.2 next.config.js/** @type {import('next').NextConfig} */constnextConfig = {  experimental: { serverActions: true },  images: {    domains: [],  },}module.exports = nextConfig7.3 tailwind.config.js â€” Key Additionsmodule.exports = {  content: ['./app/**/*.{ts,tsx}', './components/**/*.{ts,tsx}'],  theme: {    extend: {colors: {        brand: {          red:     '#F20000',          black:   '#0A0A0A',          surface: '#111111',          grey:    '#A0A0A0',        },      },fontFamily: {        sans:    ['Urbanist', 'sans-serif'],        display: ['Barlow Condensed', 'sans-serif'],      },    },  },  plugins: [],}7.4 middleware.ts// Protects all /dashboard/* routes// Redirects unauthenticated requests to /loginimport { createMiddlewareClient } from '@supabase/auth-helpers-nextjs'import { NextResponse } from 'next/server'import type { NextRequest } from 'next/server'export async function middleware(req: NextRequest) {const res = NextResponse.next()constsupabase = createMiddlewareClient({ req, res })const{ data: { session} } = await supabase.auth.getSession()  if (!session && req.nextUrl.pathname.startsWith('/dashboard')) {    return NextResponse.redirect(new URL('/login', req.url))  }  return res}export const config = {  matcher: ['/dashboard/:path*'],}8. Module Boundaries â€” What Can Import WhatThis is the most important section for keeping the codebase clean as it grows. Violating these boundaries creates circular dependencies and untestable code.ModuleCan Import Fromapp/api/*/route.tslib/* only. Never from components/. Never from other api routes.app/(dashboard)/*/page.tsxcomponents/*. hooks/*. types/*. Never from lib/ directly â€” use API routes.app/join/*/page.tsxcomponents/intake/*. components/shared/*. types/*. Never from lib/ directly.components/*components/ui/*. hooks/*. types/*. Never from lib/*. Never from app/.lib/supabase/*types/*. Nothing else from the project.lib/razorpay/*lib/supabase/server.ts. types/*. Nothing else.lib/whatsapp/*types/*. Nothing else.lib/gemini/*types/*. lib/utils/*. Nothing else.lib/cron/*lib/supabase/*. lib/whatsapp/*. lib/gemini/*. types/*.types/*Nothing â€” types are leaf nodes. No circular imports.Golden rule: data flows upward. lib/ has no knowledge of app/ or components/.If an API route needs to call another API route's logic, extract that logic into lib/ and call it from both.9. Naming ConventionsThingConventionPage filespage.tsx â€” always lowercase. No exceptions.API route filesroute.ts â€” always lowercase.Component filesPascalCase.tsx â€” e.g., StatCard.tsx, ClientTable.tsxLib filescamelCase.ts â€” e.g., create-order.ts, verify-webhook.tsType interfacesPascalCase â€” e.g., Coach, Enrollment, CheckinDatabase columnssnake_case â€” e.g., coach_id, full_name, gateway_payment_idEnvironment variablesSCREAMING_SNAKE_CASE â€” e.g., RAZORPAY_KEY_SECRETWhatsApp template namesfitosys_{function} â€” e.g., fitosys_weekly_checkinSupabase RLS policiescoaches_{verb}_{table} â€” e.g., coaches_own_clientsCron routes/api/cron/{verb}-{noun} â€” e.g., /api/cron/weekly-checkinGit branchesfeature/{short-name} or fix/{short-name}Git commitsPresent tense: 'Add Razorpay webhook handler' not 'Added...'10. Testing StrategyMVP does not require full test coverage. These are the minimum tests required before each deployment.Test TypeToolWhat to TestTypeScript compilenpxtsc --noEmitZero errors required before every commit. Non-negotiable.Webhook signatureManual with curlRazorpay webhook: valid signature accepted, invalid rejected with 400.IdempotencyManual â€” send same webhook twiceSecond payment.captured with same gateway_payment_id creates no new records.RLS isolationSupabase dashboard â€” two test coachesCoach A cannot query Coach B's clients, enrollments, or check-ins.Cron dry runPOST /api/cron/weekly-checkin with valid CRON_SECRETConfirm WhatsApp message arrives on registered test number.Razorpay test paymentTest keys + test UPIFull flow: intake form â†’ order creation â†’ test payment â†’ webhook â†’ client in DB â†’ WhatsApp received.WhatsApp verifyngrok + Meta Developer AppGET /api/webhook/whatsapp returns challenge correctly.Fitosys Project Structure v1.0  |Alchemetryx  |  March 2026  |  Confidential
