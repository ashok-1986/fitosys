FITOSYSProduct Requirements DocumentVersion 2.0March 2026  |Confidential  |  AlchemetryxDocument Change LogVersionDateChangesv1.0February 2026Initial PRD â€” Stripe payments, Make.com automation, Interakt BSP, emerald green palettev2.0March 2026Razorpay replaces Stripe. Direct Meta Cloud API replaces Interakt BSP. Native Next.js cron replaces Make.com. Red #F20000 palette confirmed. Target audience sharpened to yoga/wellness/nutrition. WhatsApp template strategy integrated. DB schema updated with gateway-agnostic naming. Build status tracker added.1. Product Overview1.1 What Is FitosysFitosys is a SaaS business operating system for independent fitness, wellness, and yoga coaches. It automates the three most time-consuming admin tasks in a coaching business: client onboarding with payment, weekly WhatsApp check-ins with AI summary, and program renewal reminders.Everything runs on WhatsApp. No app for clients to download. No behaviour change required. The coach sets up once in 30 minutes. The system runs every week automatically after that.1.2 Core PromiseA coach using Fitosys manages 40 clients with the same effort it currently takes to manage 15.1.3 Target MarketMarketSegmentDescriptionPrimaryIndiaIndependent yoga, wellness, and nutrition coaches. Tier 1 cities. 12â€“20 active clients. Currently on Google Forms + Sheets + WhatsApp.SecondaryUKSouth Asian diaspora wellness coaches. Premium pricing. Expects professional tooling.TertiaryUSA / CanadaMainstream fitness coaches. Phase 2 expansion.1.4 PricingPlanIndiaUKUSA/CanadaBasic Monthlyâ‚¹1,499/monthÂ£11/month$14/monthPro Monthlyâ‚¹2,499/monthÂ£18/month$22/monthBasic Annualâ‚¹14,999/yearÂ£110/year$135/yearMVP launches with Basic Monthly only. Pro and Annual in Phase 2.2. Tech StackAll stack decisions are final. Do not revisit without a documented reason.LayerTechnologyDecision RationaleFrontendNext.js 14 (App Router) + TypeScriptAntigravity generates excellent Next.js. Unified frontend and API in one repo.StylingTailwind CSSFast, mobile-first. Antigravity handles it natively. No custom CSS files.UI Componentsshadcn/uiAccessible, unstyled base. Overridden with Fitosys red palette.BackendNext.js API RoutesNo separate backend needed for MVP. Cron routes handle scheduling.DatabaseSupabase (PostgreSQL)Free tier generous. Row Level Security for multi-tenancy. Real-time subscriptions.AuthSupabase AuthCoach login via email/password. JWT issued by Supabase.PaymentsRazorpayIndia-first. UPI, card, net banking. Stripe ruled out â€” India availability issues.WhatsAppMeta Cloud API (Direct)No BSP. Direct integration on +91 77383 63495. Phone Number ID: 838337922704261.AIGemini API via Google AI StudioFree tier: 15 req/min, 1M tokens/day. Sufficient for MVP summary generation.HostingVercelAuto-deploy from Git. Serverless cron via vercel.json.DNSHostinger â†’ Vercelfitosys.alchemetryx.com custom domain. A record points to Vercel.EmailResendTransactional emails. Free 3,000/month.2.1 Environment VariablesNever commit these to Git. Store in .env.local for development. Add to Vercel dashboard for production.# SupabaseNEXT_PUBLIC_SUPABASE_URL=NEXT_PUBLIC_SUPABASE_ANON_KEY=SUPABASE_SERVICE_ROLE_KEY=# RazorpayRAZORPAY_KEY_ID=RAZORPAY_KEY_SECRET=RAZORPAY_WEBHOOK_SECRET=NEXT_PUBLIC_RAZORPAY_KEY_ID=        # Only public key goes here# WhatsApp (Meta Cloud API)WHATSAPP_ACCESS_TOKEN=              # Permanent System User token â€” Meta Business ManagerWHATSAPP_PHONE_NUMBER_ID=838337922704261WHATSAPP_VERIFY_TOKEN=fitosys_whatsapp_verify_2026# AIGEMINI_API_KEY=# AppNEXT_PUBLIC_APP_URL=https://fitosys.alchemetryx.comCRON_SECRET=                        # Random string â€” validates cron route calls3. MVP Scope3.1 Phase 1 Features (In Scope)Client Onboarding with Payment (Razorpay)Weekly WhatsApp Check-in Automation with Gemini AI SummaryRenewal Reminder Automation3.2 Explicitly Out of Scope â€” Phase 1Mobile appGroup coaching or cohort managementExercise library or workout builderNutrition loggingVideo calls or lesson deliveryCoach-to-coach marketplaceCustom domain per coachZapier or third-party integrationsAPI access for coachesWhite labelingProgress photo uploadCustom check-in questions per program3.3 Build Status TrackerModuleStatusNotesSupabase schema + RLSCOMPLETEGateway-agnostic column naming. client_id nullable on enrollments.Gemini AI lib (lib/gemini/)COMPLETE5 files: client.ts, weekly-summary.ts, risk-score.ts, renewal-message.ts, coach-insight.ts. Zero tsc errors.WhatsApp lib (lib/whatsapp/)COMPLETEsend.ts, templates.ts, webhook route. Direct Meta Cloud API.Razorpay KYCCOMPLETELive keys active. Test keys (rzp_test_ prefix) used in dev.WhatsApp templatesIN REVIEW3 templates submitted Mar 6 2026: fitosys_weekly_checkin, fitosys_renewal_reminder, fitosys_client_welcome.Coach auth (login/signup)PENDINGCoach dashboardPARTIALShell exists. Not wired to real data.Client onboarding flowPENDINGNext priority. 4 Antigravity tasks defined in Section 7.Razorpay webhook handlerPENDINGWeekly check-in cronPENDINGGemini Monday summaryPENDINGRenewal reminder cronPENDINGT3 welcome template resubmit as UtilityPENDINGCurrently submitted as Marketing. Must resubmit.T2A renewal info Utility templatePENDINGNew template. Not yet submitted.Permanent Meta System User tokenPENDINGRequired before go-live.Interakt verify token setupPENDINGWHATSAPP_VERIFY_TOKEN=fitosys_whatsapp_verify_20264. Database SchemaAll tables use UUID primary keys. All coach-specific tables include coach_id for RLS enforcement. Column naming is gateway-agnostic â€” no stripe_ or razorpay_ prefixes on shared fields.4.1 coachesCREATE TABLE coaches (  id                UUID PRIMARY KEY DEFAULT gen_random_uuid(),  email             TEXT UNIQUE NOT NULL,full_name         TEXT NOT NULL,whatsapp_number   TEXT NOT NULL,country_code      TEXT NOT NULL DEFAULT 'IN',timezone          TEXT NOT NULL DEFAULT 'Asia/Kolkata',coaching_typeTEXT[] DEFAULT '{}',gateway_customer_idTEXT,         -- Razorpay customer ID  plan              TEXT DEFAULT 'trial',checkin_day       INTEGER DEFAULT 0,  -- 0=Sunday â€¦ 6=Saturdaycheckin_time      TEXT DEFAULT '19:00',  slug              TEXT UNIQUE NOT NULL,  status            TEXT DEFAULT 'active',created_at        TIMESTAMPTZ DEFAULT NOW(),updated_at        TIMESTAMPTZ DEFAULT NOW());4.2 programsCREATE TABLE programs (  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),coach_id        UUID REFERENCES coaches(id) ON DELETE CASCADE NOT NULL,  name            TEXT NOT NULL,  description     TEXT,duration_weeks  INTEGER NOT NULL,  price           DECIMAL(10,2) NOT NULL,  currency        TEXT NOT NULL DEFAULT 'INR',checkin_type    TEXT DEFAULT 'wellness',is_active       BOOLEAN DEFAULT TRUE,created_at      TIMESTAMPTZ DEFAULT NOW());4.3 clientsCREATE TABLE clients (  id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),coach_id            UUID REFERENCES coaches(id) ON DELETE CASCADE NOT NULL,full_name           TEXT NOT NULL,whatsapp_number     TEXT NOT NULL,  email               TEXT NOT NULL,  age                 INTEGER,primary_goal        TEXT,health_notes        TEXT,gateway_customer_idTEXT,         -- Razorpay customer ID  status              TEXT DEFAULT 'active',  -- active | inactive | trialcreated_at          TIMESTAMPTZ DEFAULT NOW());4.4 enrollmentsCREATE TABLE enrollments (  id                        UUID PRIMARY KEY DEFAULT gen_random_uuid(),coach_id                  UUID REFERENCES coaches(id) NOT NULL,client_id                 UUID REFERENCES clients(id),  -- nullable: set after client createdprogram_id                UUID REFERENCES programs(id) NOT NULL,start_date                DATE NOT NULL,end_date                  DATE NOT NULL,amount_paidDECIMAL(10,2) NOT NULL,  currency                  TEXT NOT NULL DEFAULT 'INR',gateway_payment_idTEXT,         -- Razorpay payment IDgateway_order_idTEXT,         -- Razorpay order ID  status                    TEXT DEFAULT 'active',  -- active | expired | renewed | cancelled  renewal_reminder_1_sent   BOOLEAN DEFAULT FALSE,  renewal_reminder_2_sent   BOOLEAN DEFAULT FALSE,churn_reason              TEXT,created_at                TIMESTAMPTZ DEFAULT NOW());4.5 checkinsCREATE TABLE checkins (  id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),coach_id            UUID REFERENCES coaches(id) NOT NULL,client_id           UUID REFERENCES clients(id) NOT NULL,enrollment_id       UUID REFERENCES enrollments(id),week_number         INTEGER NOT NULL,check_date          DATE NOT NULL,raw_reply           TEXT,sessions_completed  INTEGER,energy_score        INTEGER CHECK (energy_score >= 1 AND energy_score <= 10),  notes               TEXT,  processed           BOOLEAN DEFAULT FALSE,  -- FALSE until Gemini summary runresponded_at        TIMESTAMPTZ,message_sent_at     TIMESTAMPTZ,created_at          TIMESTAMPTZ DEFAULT NOW());4.6 ai_summariesCREATE TABLE ai_summaries (  id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),coach_id            UUID REFERENCES coaches(id) NOT NULL,week_start_date     DATE NOT NULL,week_end_date       DATE NOT NULL,summary_text        TEXT NOT NULL,total_clients       INTEGER,responded_count     INTEGER,avg_energy_scoreDECIMAL(3,1),generated_at        TIMESTAMPTZ DEFAULT NOW(),delivered_to_coach  BOOLEAN DEFAULT FALSE);4.7 paymentsCREATE TABLE payments (  id                    UUID PRIMARY KEY DEFAULT gen_random_uuid(),coach_id              UUID REFERENCES coaches(id) NOT NULL,client_id             UUID REFERENCES clients(id) NOT NULL,enrollment_id         UUID REFERENCES enrollments(id),  amount                DECIMAL(10,2) NOT NULL,  currency              TEXT NOT NULL DEFAULT 'INR',payment_type          TEXT NOT NULL,  -- new | renewalgateway_payment_id    TEXT UNIQUE NOT NULL,  --Razorpay payment ID (idempotency key)gateway_order_id      TEXT,gateway_status        TEXT,paid_at               TIMESTAMPTZ,created_at            TIMESTAMPTZ DEFAULT NOW());4.8 whatsapp_logCREATE TABLE whatsapp_log (  id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),coach_id            UUID REFERENCES coaches(id),client_id           UUID REFERENCES clients(id),  direction           TEXT NOT NULL,  -- outbound | inboundmessage_typeTEXT,  --checkin | renewal | welcome | coach_notification | summarymessage_content     TEXT,whatsapp_message_id TEXT,  status              TEXT,  -- sent | delivered | read | failedsent_at             TIMESTAMPTZ DEFAULT NOW());4.9 Row Level Security Policies-- Apply to all tables with coach_idALTER TABLE coaches     ENABLE ROW LEVEL SECURITY;ALTER TABLE programs    ENABLE ROW LEVEL SECURITY;ALTER TABLE clients     ENABLE ROW LEVEL SECURITY;ALTER TABLE enrollments ENABLE ROW LEVEL SECURITY;ALTER TABLE checkins    ENABLE ROW LEVEL SECURITY;ALTER TABLE ai_summaries ENABLE ROW LEVEL SECURITY;ALTER TABLE payments    ENABLE ROW LEVEL SECURITY;ALTER TABLE whatsapp_log ENABLE ROW LEVEL SECURITY;-- Example policy (repeat for each table)CREATE POLICY coaches_own_clients ON clients  FOR ALL USING (coach_id = auth.uid());-- Public intake â€” no RLS bypass needed-- Use service_role key only in webhook handlers and cron routes5. WhatsApp Template RegistryAll templates submitted via Meta WhatsApp Manager. Direct Cloud API on +91 77383 63495. No BSP. Phone Number ID: 838337922704261.Template NameCategoryRate (India)Statusfitosys_weekly_checkinMARKETINGâ‚¹1.09/msgActive â€” In Review (Mar 6)fitosys_renewal_reminderMARKETINGâ‚¹1.09/msgActive â€” In Review (Mar 6)fitosys_client_welcomeMARKETING â†’ resubmit as UTILITYâ‚¹0.145/msgResubmit requiredfitosys_renewal_infoUTILITYâ‚¹0.145/msgNot yet submittedfitosys_coach_otpAUTHENTICATIONâ‚¹0.145/msgPhase 2 onlyCost at 200 clients: ~â‚¹887/month. At 500 clients: ~â‚¹2,231/month.24-hour free service window opens every time a client replies. Build onboarding so clients reply first â€” this is the highest-ROI cost optimisation available.6. API Route Registry6.1 Auth RoutesPOST  /api/auth/signup    -- Coach signup. Creates coach record + slug.POST  /api/auth/login     -- Coach login. Returns Supabase JWT.POST  /api/auth/logout    -- Invalidates session.GET   /api/auth/me        -- Returns current coach profile.6.2 Coach RoutesGET   /api/coaches/profile    -- Get coach profilePUT   /api/coaches/profile    -- Update coach profilePUT   /api/coaches/settings   -- Update checkin day / time / timezoneGET   /api/coaches/dashboard  -- Dashboard summary data (see Section 8.1)6.3 Program RoutesGET    /api/programs              -- List all programs for coachPOST   /api/programs              -- Create new programPUT    /api/programs/:id          -- Edit programDELETE /api/programs/:id          -- Deactivate (soft delete)GET    /api/programs/public/:slug -- Public: coach programs for intake form (no auth)6.4 Client RoutesGET    /api/clients               -- List all clients with filtersGET    /api/clients/:id           -- Single client with full historyPUT    /api/clients/:id           -- Update client detailsGET    /api/clients/:id/checkins  -- Client check-in historyGET    /api/clients/:id/payments  -- Client payment history6.5 Enrollment RoutesGET  /api/enrollments              -- List active enrollmentsGET  /api/enrollments/expiring     -- Enrollments expiring in 7 daysPUT  /api/enrollments/:id/churn    -- Log churn reason6.6 Check-in RoutesGET  /api/checkins/weekly           -- All check-ins for current weekGET  /api/checkins/summary/:week    -- AI summary for a given weekPOST /api/checkins/trigger          -- Manual trigger (testing only)6.7 Payment RoutesGET  /api/payments                  -- List payments with date filterPOST /api/payments/create-order     -- Create Razorpay order for clientPOST /api/payments/create-renewal   -- Create Razorpay renewal link6.8 Webhook RoutesThese routes require NO auth JWT. They verify signatures independently.POST /api/webhook/razorpay    -- Razorpay payment eventsPOST /api/webhook/whatsapp   -- Incoming WhatsApp replies (POST)GET  /api/webhook/whatsapp   -- Meta webhook verification (GET)6.9 Public RoutesGET  /api/public/:slug         -- Coach intake page dataPOST /api/public/:slug/intake  -- Submit intake form + create Razorpay order6.10 Cron RoutesAll cron routes verify x-cron-secret header against CRON_SECRET env var before executing.POST /api/cron/weekly-checkin   -- Sunday 7PM IST â€” fires T1 to all active clientsPOST /api/cron/renewal-check    -- Daily 10AM UTC â€” fires T2A at 7 days remainingPOST /api/cron/renewal-followup -- Daily 10AM UTC â€” fires T2B if T2A no reply after 48hPOST /api/cron/monday-summary   -- Monday 7AM IST â€” Gemini summary + WhatsApp to coachPOST /api/cron/expiry-check     -- Daily midnight UTC â€” moves expired enrollments to inactive7. Feature Specifications7.1 Feature 1 â€” Client Onboarding with PaymentEach coach gets a unique link at fitosys.alchemetryx.com/join/[slug]. The client fills an intake form, selects a program, and pays via Razorpay â€” UPI, card, or net banking. Everything on one page. No redirects to another platform.User StoriesCoach: I want a shareable onboarding link so clients can onboard without me guiding them step by step.Coach: I want to receive a WhatsApp notification the moment a client pays so I can send a personal welcome.Coach: I want the client to appear in my dashboard immediately after payment with no manual entry.Client: I want to fill one form and pay in one step. The process should take under 5 minutes.Client: I want a WhatsApp confirmation immediately after payment so I know I am registered.Intake Form FieldsFieldTypeNotesFull NameText â€” requiredWhatsApp NumberTel â€” requiredWith country code. Used as client identifier.Email AddressEmail â€” requiredAgeNumber â€” requiredPrimary GoalSingle select â€” requiredWeight Loss / Muscle Gain / Flexibility / Overall Fitness / Mental Wellness / Spiritual PracticeHealth ConditionsFree text â€” optionalProgram SelectionRadio â€” requiredRenders coach's active programs with priceTerms AgreementCheckbox â€” requiredPayment FlowClient submits intake form â†’ POST /api/public/:slug/intakeAPI validates fields, creates Razorpay order, returns orderId + amountFrontend loads Razorpay checkout.js, opens modal with orderIdClient pays. Razorpay fires payment.captured webhook to /api/webhook/razorpayWebhook: verify signature â†’ idempotency check â†’ create client â†’ create enrollment â†’ create payment record â†’ fire T3 WhatsApp to client â†’ fire freeform notification to coachClient redirected to /join/[slug]/success â€” terminal pageAcceptance CriteriaOnboarding link live within 24 hours of coach account creationClient completes form and pays in under 5 minutesWhatsApp notifications sent within 60 seconds of payment.capturedClient appears in coach dashboard within 2 minutes of paymentFailed payment creates no orphan records in databaseRazorpay webhook handles duplicate payment IDs idempotently â€” check gateway_payment_id before processing7.2 Feature 2 â€” Weekly Check-in Automation with AI SummaryEvery Sunday at 7 PM IST (per coach timezone config), T1 check-in message fires to all active clients. Clients reply on WhatsApp. Replies are stored. Every Monday at 7 AM IST, Gemini processes all responses and delivers a structured summary to the coach.Check-in Message Template (T1)Hi {{1}}, it's your weekly check-in with Coach {{2}} ðŸ’ªQuick check-in â€” reply with your answers:1ï¸âƒ£ Energy this week (1â€“10)?2ï¸âƒ£ Sessions completed?3ï¸âƒ£ One win this week?4ï¸âƒ£ Anything challenging you?Takes 60 seconds. Your coach reads every reply.Gemini Prompt StructurePrompt sent to Gemini API every Monday at 7 AM. Temperature: 0.3. Max tokens: 512. Model: gemini-2.0-flash.Output must include exactly these sections: RESPONSE RATE / NEEDS ATTENTION (up to 3) / STRONG PROGRESS (up to 3) / GROUP AVERAGE / THIS WEEK PRIORITY / NON-RESPONDERS. Total under 150 words. No filler phrases.Acceptance CriteriaCheck-in messages sent within 5-minute window of configured timeClient reply matched to correct client via WhatsApp phone numberMultiple messages from same client concatenated into single checkins recordGemini summary generated and delivered by 7:15 AM MondayIf Gemini fails: retry once after 4 seconds, once after 16 seconds, once after 60 seconds. After third failure: store as failed, notify coach of delay.Coach can view historical summaries for any previous week in dashboard7.3 Feature 3 â€” Renewal Reminder AutomationDaily cron at 10 AM UTC checks for programs ending in 7 days. Sends T2A (Utility, factual info, no payment link). If no reply within 48 hours, fires T2B (Marketing, with Razorpay payment link). If still unpaid after program end, flags coach in dashboard.Renewal SequenceStepTemplateTriggerCostT-7 daysT2A fitosys_renewal_info (UTILITY)Daily cron, renewal_reminder_1_sent = falseâ‚¹0.145/msgT-7 + 48h (no reply)T2B fitosys_renewal_reminder (MARKETING)renewal_reminder_2_sent = false AND 48h elapsedâ‚¹1.09/msgClient replies to T2ASend payment link inside free 24h windowWhatsApp webhook reply detectedâ‚¹0.00Client paysRazorpay webhook extends end_datepayment.captured eventâ€”No payment after program end + 3 daysFlag in dashboard as churnexpiry-check cronâ€”Acceptance CriteriaNo duplicate renewal messages within same 7-day window â€” check renewal_reminder_1_sent flagRazorpay payment link in T2B is pre-filled with correct amount and client metadataSuccessful renewal extends end_date by one program duration from payment date â€” not restart from todayCoach notification sent within 60 seconds of renewal paymentChurn logging is optional but prompted in dashboard8. Dashboard Specifications8.1 /api/coaches/dashboard Response{active_clients: number,revenue_this_month: number,  currency: string,renewals_due_this_week: number,checkin_response_rate_last_week: number,  // 0.0 â€“ 1.0clients_needing_attention: [{    id: string,    name: string,    reason: string,last_energy_score: number | null  }],last_summary_preview: string,  renewals: [{client_name: string,    program: string,end_date: string,days_remaining: number  }]}8.2 Navigation StructureRoutePage/dashboardHome â€” stat cards + attention list + renewals + summary preview/dashboard/clientsClient list â€” table with filters, search, click-to-profile/dashboard/clients/:idClient profile â€” overview, check-in history, payment history/dashboard/programsPrograms â€” list, toggle active, add new, copy onboarding link/dashboard/pulseWeekly Pulse â€” current AI summary, 8-week response rate chart, history/dashboard/paymentsPayments â€” monthly revenue chart, transaction list, date filter/dashboard/settingsSettings â€” profile, check-in config, billing, WhatsApp setup8.3 Design TokensTokenValueUsagePrimary#F20000CTAs, active states, key data points. Use sparingly.Background#0A0A0APage background. Fitosys lives in the dark.Surface#111111Cards, panels, sidebars.Text Primary#FFFFFFHeadings, important values.Text Secondary#A0A0A0Labels, supporting text.Warning#F59E0BRenewal due, attention needed.Danger#EF4444Overdue, churn, critical.Success#10B981Paid, active, confirmed.Font â€” BodyUrbanist (Google Fonts)All UI text. Weights: 300, 400, 600, 700.Font â€” DisplayBarlow CondensedHero text, section headers, stat numbers.9. Scheduled JobsAll cron jobs run as Next.js API routes triggered by Vercel Cron. Configured in vercel.json. Each route validates x-cron-secret header before executing.// vercel.json{  "crons": [{ "path": "/api/cron/weekly-checkin",   "schedule": "0 13 * * 0" },  // Sunday 7PM IST (13:30 UTC){ "path": "/api/cron/renewal-check",    "schedule": "0 10 * * *" },  // Daily 10AM UTC{ "path": "/api/cron/renewal-followup", "schedule": "0 11 * * *" },  // Daily 11AM UTC{ "path": "/api/cron/monday-summary",   "schedule": "30 1 * * 1" },  // Monday 7AM IST (1:30 UTC){ "path": "/api/cron/expiry-check",     "schedule": "0 0 * * *" }   // Daily midnight UTC  ]}Cron RouteScheduleAction/api/cron/weekly-checkinSunday 7PM ISTQuery all coaches. For each: fetch active clients, send T1 check-in via WhatsApp. Log to whatsapp_log./api/cron/renewal-checkDaily 10AM UTCQuery enrollments: end_date between today and today+7, status=active, reminder_1_sent=false. Send T2A. Set reminder_1_sent=true./api/cron/renewal-followupDaily 11AM UTCQuery enrollments: reminder_1_sent=true, reminder_2_sent=false, reminder sent 48h+ ago, no payment since. Send T2B. Set reminder_2_sent=true./api/cron/monday-summaryMonday 7AM ISTFor each coach: fetch last week's checkins. Call Gemini. Store in ai_summaries. Send WhatsApp to coach. Set delivered_to_coach=true./api/cron/expiry-checkDaily midnight UTCSet enrollments to inactive where end_date < today AND status=active AND no payment in 3 days. Prompt coach to log churn reason.10. Webhook Specifications10.1 Razorpay Webhook â€” /api/webhook/razorpayEvents Handledpayment.captured â€” primary event. Creates client + enrollment + payment records. Fires WhatsApp messages.payment.failed â€” logs failure. Flags in dashboard if this was a renewal attempt.Processing Order for payment.capturedVerify Razorpay signature using RAZORPAY_WEBHOOK_SECRETExtract payment.id â€” check payments table for existing record (idempotency)Extract metadata from payment notes: coach_slug, program_id, client intake dataUpsert client record on (whatsapp_number, coach_id)Create enrollment record with start_date=today, end_date=today+duration_weeksCreate payment record with gateway_payment_idSend T3 WhatsApp welcome to clientSend freeform WhatsApp notification to coachReturn 200 â€” always return 200 to Razorpay even on partial failuresCritical: always return 200 to Razorpay. If you return 4xx/5xx, Razorpay will retry and may create duplicate records. Handle all errors internally and log them.10.2 WhatsApp Webhook â€” /api/webhook/whatsappGET â€” VerificationMeta sends: hub.mode, hub.verify_token, hub.challengeVerify hub.verify_token === WHATSAPP_VERIFY_TOKENReturn hub.challenge as plain text with 200POST â€” Incoming MessagesExtract sender phone number from entry[0].changes[0].value.messages[0].fromQuery clients: SELECT * FROM clients WHERE whatsapp_number = [number]If match: check for open checkin this week. Append reply to checkins.raw_reply. Set processed=false. Set responded_at=NOW().If no match: log to whatsapp_log with client_id=nullAlways return 20011. Antigravity Build SequenceBuild in this exact order. Do not start the next task until the current one has zero TypeScript errors (npxtsc --noEmit) and the core flow works end to end.TaskModuleAntigravity Prompt Summary1Public intake pageCreate app/join/[slug]/page.tsx. Fetches GET /api/public/:slug. Renders intake form with coach programs. On submit POSTs to /api/public/:slug/intake. Gets back RazorpayorderId. Loads checkout.js and opens payment modal. On success redirects to /join/[slug]/success.2Public API routesCreate GET /api/public/[slug]/route.ts â€” returns coach + active programs, no auth. Create POST /api/public/[slug]/intake/route.ts â€” validates form, creates Razorpay order via SDK, returns orderId. Does NOT create DB records yet.3Razorpay webhook handlerCreate app/api/webhook/razorpay/route.ts. Verify signature. Idempotency on gateway_payment_id. Create client (upsert), enrollment, payment. Fire T3 WhatsApp + coach notification. Always return 200.4Success pageCreate app/join/[slug]/success/page.tsx. Terminal page. Shows confirmation, program name, start date. No navigation links out. Tells client to expect WhatsApp message.5Wire dashboard to real dataConnect GET /api/coaches/dashboard to live Supabase queries. Replace all mock data. Add loading states.6Weekly check-in cronCreate app/api/cron/weekly-checkin/route.ts. Validates cron secret. Queries active coaches and clients. Calls sendWhatsAppTemplate(T1) for each. Logs to whatsapp_log.7Monday Gemini summaryCreate app/api/cron/monday-summary/route.ts. Fetches last week's checkins per coach. Calls lib/gemini/weekly-summary.ts. Stores in ai_summaries. Sends WhatsApp to coach.8Renewal cronCreate renewal-check and renewal-followupcron routes. Wire T2A and T2B templates. Update renewal flags in enrollments table.12. Non-Functional Requirements12.1 PerformanceDashboard loads under 2 seconds on 4G mobileWebhook handlers respond under 5 seconds (Razorpay timeout is 5s)WhatsApp messages sent within 60 seconds of trigger eventGemini summary generation under 30 seconds per coach12.2 SecurityAll API routes authenticated via Supabase JWT except /api/public/* and /api/webhook/*Razorpay webhook: verify x-razorpay-signature header before processingWhatsApp webhook: verify hub.verify_token on GET, validate sender origin on POSTCron routes: verify x-cron-secret header â€” reject all others with 401No coach can access another coach's data â€” enforced at both RLS and application layerNEXT_PUBLIC_ prefix only on variables that must be exposed to the browser (Razorpay key ID, Supabase URL/anon key)12.3 ReliabilityRazorpay webhook handler is idempotent â€” duplicate events safe via gateway_payment_id checkWhatsApp send failures logged to whatsapp_log but do not crash other operationsGemini API failure falls back with 3 retries (4s / 16s / 60s). After third: mark failed, notify coach.Cron job failures alert to founder's WhatsApp12.4 ScalabilityArchitecture handles 500 coaches / 20,000 clients without changesMove to Supabase paid tier ($25/month) at approximately 100 coachesVercel free tier sufficient for MVP. Scale at 1,000 daily active users.13. Launch Checklist13.1 TechnicalAll 3 features working end to end in stagingMulti-tenant data isolation tested with 2 dummy coach accountsRazorpay webhook tested with test keys locallyWhatsApp check-in tested with real replies on registered numberGemini summary tested with real check-in dataRenewal flow tested with manual cron triggerMobile responsive â€” tested on iPhone and Androidnpxtsc --noEmit â€” zero errors before every deploy13.2 WhatsApp3 submitted templates complete Meta reviewfitosys_client_welcome resubmitted as UTILITY templatefitosys_renewal_info submitted as new UTILITY templatePermanent System User access token generated in Meta Business ManagerWHATSAPP_ACCESS_TOKEN and WHATSAPP_PHONE_NUMBER_ID added to Vercel envWebhook URL registered in Meta Developer App: https://fitosys.alchemetryx.com/api/webhook/whatsappIndia payment method added in Meta Billing Hub13.3 Businessfitosys.alchemetryx.com domain active and pointing to VercelRazorpay live keys active â€” KYC cleared âœ“Gemini API key active in Google AI StudioTerms of Service page livePrivacy Policy page live5 beta coaches confirmedFirst real coach onboarded and activeFitosys PRD v2.0  |Alchemetryx  |  March 2026  |  Confidential
